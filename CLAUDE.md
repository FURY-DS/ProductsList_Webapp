# ProductsList_Webapp — 작업 가이드

한국 이커머스 상품 관리 웹앱. 바닐라 HTML/CSS/JS (빌드 없음), 단일 사용자용.
Tencent WorkBuddy로 대부분 만들어졌고, Claude Code가 버그 수정/검토/신규 기능을
이어서 작업 중. `.workbuddy/memory/`에 WorkBuddy 쪽 작업 기록이 있지만
**스테일할 수 있음** — 아래 "메모 신뢰하지 않기" 참고.

## 절대 규칙 (매번 지킬 것)

1. **`main`에 직접 커밋/푸시하지 않는다.** Cloudflare Pages + GitHub가 연동돼
   `main`이 사실상 프로덕션이다. 항상 새 브랜치에서 작업하고, push/PR 전에는
   반드시 먼저 물어본다. `git checkout -b <설명적인-브랜치명>`으로 시작.
2. **"고쳤다"고 말하기 전에 실제로 검증한다.**
   - JS 문법: `node --check <file>` (모든 수정 파일에 대해)
   - 실제 동작: 가능하면 브라우저로 열어서 확인 (Claude_Browser 도구로 로컬
     서버 띄우고 콘솔 에러 없는지, 화면이 실제로 뜨는지 확인)
   - "코드가 맞아 보인다"만으로 끝내지 않는다 — 과거 이 프로젝트에서 기계적
     일괄 수정이 중괄호 하나 빠뜨려서 5개 페이지를 통째로 먹통 만든 사고가
     실제로 있었다 (2026-07-26, storage.js 브레이스 누락).
3. **10개 페이지 복제 구조를 건드릴 땐 범위부터 확인한다.** 아래 "페이지
   구조" 참고. 한 페이지의 로직/필드/버그를 고치면 "이거 다른 9개 페이지에도
   해당하나?"를 먼저 사용자에게 확인하거나, 코드로 직접 확인 후 명시한다.
   무심코 4개만 고치고 나머지를 빠뜨리는 게 가장 흔한 사고 패턴이다.
4. **Cloudflare Functions(`functions/`)를 건드릴 땐 특히 조심한다.** 실제
   로그인/세션/사용자 데이터가 걸려있는 프로덕션 백엔드다. KV에 직접 쓰기/
   삭제하는 명령은 절대 실행하지 않는다 (읽기 전용 review는 항상 안전).
   배포(`wrangler deploy` 등)도 실행하지 않는다 — 이 저장소는 GitHub 연동
   auto-deploy 방식으로 보이므로, 브랜치 push 후 PR을 만들어 Cloudflare
   Pages의 **미리보기(preview) URL**로 먼저 테스트하는 걸 기본 경로로 삼는다.

## 페이지 구조 (복제 패턴 — 사고 나기 제일 쉬운 지점)

**10개 판매/관리 페이지**, 페이지당 7개 JS 파일(`config/state/storage/search/
card/actions/app`) = 총 70개 파일이 사실상 하나의 로직을 복제 구현:

- **8페이지 호환 그룹** (`COMPATIBLE_PAGES`로 서로 JSON 가져오기 호환):
  스마트스토어(smartstore), 쿠팡(coupang), ESM(esm), 11번가(elevenst),
  오너클랜(ownerclan), 도매꾹(domagguk), 올웨이즈(always), 토스쇼핑(tossshopping)
  - 이 중 **도매꾹/올웨이즈/토스쇼핑 3개는 스마트스토어와 진짜 100% 동일** (식별자 이름만 다름)
  - **오너클랜만 다름**: `공급가입력(supplyPrice)` 필드가 있고
    `판매가 = 공급가 × 1.1`로 자동 계산됨. 수수료/이익도 공급가 기준.
    (`.workbuddy/memory/MEMORY.md`는 "100% 동일"이라 적혀 있지만 **틀렸음** —
    실제 코드(`js/ownerclan-card.js`)를 기준으로 삼을 것)
- **N배송(nshipping) / 로켓그로스(rocketgrowth)**: 호환 그룹에서 **분리**되어
  있음 (서로/다른 페이지와 JSON 가져오기 불가, 의도된 설계).
  - N배송만 있는 필드: `barcodeFee`, `pickingFee`, `tagFee` (NY바코드/피킹/태그 비용)
  - `warehouseFee`/`marketFee` 필드 키는 **페이지마다 다른 실제 비용을 가리킴**
    (스마트스토어="창고택배비/마켓택배비", 로켓그로스="피킹라벨출고비/
    쿠팡입출고비용") — 같은 키, 다른 의미. 최종이익 계산식도 로켓그로스만
    `marketFee` 부호가 반대 (더하기 아닌 빼기).
- **상품리스트(index.html)**: 마스터 페이지, 다른 페이지와 다른 독자 구조
  (판매가/수수료 없음, 원가×환율×퍼센트=총합만 계산). 이미지 원본이 여기
  저장되고 다른 페이지들이 `판매자상품코드`로 조회해감.

**공유 요소 (겉보기엔 독립적이지만 실제로 결합돼 있음)**:
- 8페이지 호환 그룹 + N배송/로켓그로스까지 전부 `css/smartstore.css` 하나만
  참조하고, DOM class도 리터럴 `smartstore-card`/`smartstore-calc-rows`를
  그대로 씀 (페이지별로 안 바뀜, 의도된 설계). 이 CSS를 "스마트스토어만
  고치려고" 건드리면 9개 페이지가 동시에 영향받는다.

**알려진 사소한 불일치** (기능엔 무해, 복제 과정 흔적):
- `js/coupang-card.js`에만 어디서도 쓰이지 않는 죽은 CSS 클래스
  (`coupang-card`, `coupang-calc-rows`)가 붙어있음.
- 함수 명명 규칙이 파일마다(심지어 한 파일 안에서도) 최소 3가지로 섞여있음
  (접두어형/접미어형/"For-X"형). 새 코드 작성 시 해당 파일의 기존 패턴을
  따르되, 굳이 통일하려 하지 않는다 (범위 벗어난 리팩토링 금지, 아래 참고).

## 백엔드 (functions/) — Cloudflare Pages Functions + KV

- 인증 공통 로직: `functions/_lib/auth.js`
- KV 키 구조: `user:<username>`, `session:<token>` (TTL 30일),
  `data:<username>`, `ratelimit:<bucket>:<ip>` (TTL로 자동 만료)
- 관리자 인증 2가지 방식: `X-Admin-Key` 헤더(마스터 키, env.ADMIN_KEY) 또는
  `role: "admin"` 계정의 Bearer 토큰. 마스터 키는 어디에도 하드코딩되어
  있지 않음 — `admin.html`에서 사용자가 직접 입력해 sessionStorage에만 저장.
- 로그인/가입에는 IP 기준 rate limit 적용됨 (로그인 실패 10회/15분, 가입
  5회/1시간, `functions/_lib/auth.js`의 `isRateLimited`/`recordAttempt`).
  이 임계값을 낮추면 실사용자가 락아웃될 수 있으니 바꿀 땐 신중히.
- 사용자별 데이터는 세션에서 검증된 username으로 KV 키를 만듦 (`/api/data`)
  — 클라이언트가 보낸 값으로 다른 사용자 키에 접근 못 하게 이 패턴 유지할 것.

## 메모(.workbuddy/memory) 신뢰하지 않기

WorkBuddy가 남긴 일자별 메모는 유용하지만 **틀린 내용이 실제로 있었다**
(오너클랜 "100% 동일" 주장이 대표 사례). 메모에 적힌 내용을 근거로 코드를
바꾸기 전에, 반드시 해당 소스 파일을 직접 읽어 확인한다. 메모는 "당시
의도/맥락"을 이해하는 용도로만 쓰고, "현재 사실"의 근거로 쓰지 않는다.

## 범위 관리

- 요청받지 않은 리팩토링/추상화를 먼저 하지 않는다. 예: 10개 페이지의
  중복을 발견해도, 명시적으로 리팩토링을 요청받기 전에는 구조를 바꾸지
  않는다 (버그 수정과 리팩토링은 분리해서 각각 확인받는다).
- 버그를 발견했는데 지금 요청 범위 밖이면, 고치지 말고 먼저 보고한다.
