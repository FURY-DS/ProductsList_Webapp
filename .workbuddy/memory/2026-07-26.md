# 2026-07-26

## v35 - 메뉴 드랍다운에 오너클랜/도매꾹 비활성화 항목 추가 (2026-07-26 12:06)
- js/config.js MENU_ITEMS에 두 항목 추가:
  - { page: 'ownerclan', label: '오너클랜', url: '#', ready: false }
  - { page: 'domagguk', label: '도매꾹', url: '#', ready: false }
- ready: false → menu.js에서 coming-soon 클래스 적용 → 클릭 시 "준비 중" 토스트
- url: '#' → 클릭 시 페이지 이동 없음
- ESM/11번가 뒤에 위치 (메뉴 순서: 상품리스트 → N배송 → 로켓그로스 → 스마트스토어 → 쿠팡 → ESM/11번가 → 오너클랜 → 도매꾹)

## v36 - 메뉴 드랍다운에 올웨이즈 비활성화 항목 추가 (2026-07-26 13:37)
- js/config.js MENU_ITEMS에 항목 추가:
  - { page: 'always', label: '올웨이즈', url: '#', ready: false }
- ready: false → menu.js에서 coming-soon 클래스 적용 → 클릭 시 "준비 중" 토스트
- 도매꾹 뒤에 위치 (메뉴 순서: 상품리스트 → N배송 → 로켓그로스 → 스마트스토어 → 쿠팡 → ESM/11번가 → 오너클랜 → 도매꾹 → 올웨이즈)

## v37 - 메뉴 드랍다운에 토스쇼핑 비활성화 항목 추가 (2026-07-26 13:58)
- js/config.js MENU_ITEMS에 항목 추가:
  - { page: 'tossshopping', label: '토스쇼핑', url: '#', ready: false }
- ready: false → menu.js에서 coming-soon 클래스 적용 → 클릭 시 "준비 중" 토스트
- 올웨이즈 뒤에 위치 (메뉴 순서: ...→ 오너클랜 → 도매꾹 → 올웨이즈 → 토스쇼핑)

## v38 - ESM/11번가 페이지를 ESM과 11번가로 분리 (2026-07-26 14:53)
- 요청: ESM/11번가를 별개의 ESM, 11번가 두 페이지로 분리, JSON 4개 호환 그룹 구성
- 작업:
  - esm11-* 파일들을 복제하여 esm-* 7개 + elevenst-* 7개 JS 파일 생성 (Python 일괄치환)
  - esm.html, elevenst.html 생성 (스크립트 참조 및 타이틀 변경)
  - js/config.js MENU_ITEMS: esm11 항목 → esm + elevenst 두 항목으로 교체
  - COMPATIBLE_PAGES 4개 config 모두 ['smartstore', 'coupang', 'esm', 'elevenst']로 통일
  - esm-config.js: PAGE_ID='esm', STORAGE_KEY='esm_v1', PAGE_TITLE='ESM'
  - elevenst-config.js: PAGE_ID='elevenst', STORAGE_KEY='elevenst_v1', PAGE_TITLE='11번가'
  - 예전 esm11.html + js/esm11-*.js 8개 파일 삭제
  - CSS 클래스 smartstore-card, smartstore-calc-rows, css/smartstore.css 참조 보존
- 메뉴 순서: 상품리스트 → N배송 → 로켓그로스 → 스마트스토어 → 쿠팡 → ESM → 11번가 → 오너클랜 → 도매꾹 → 올웨이즈 → 토스쇼핑
- JSON 호환: 스마트스토어/쿠팡/ESM/11번가 4개 페이지 상호 가져오기 가능
- 활성 7개 + 비활성 4개 = 총 11개 메뉴, JS 파일 54개

## v39 - 판매수수료 전체입력 기능 추가 (2026-07-26 15:07)
- 요청: 6개 판매 페이지(N배송/로켓그로스/스마트스토어/쿠팡/ESM/11번가)에 판매수수료 일괄 변경 기능
- 작업:
  - js/modal.js: showModal에 confirmText 파라미터 추가 (기본값 '확인')
  - css/smartstore.css: .bulk-fee-bar 스타일 추가 (라벨+입력+적용버튼)
  - 6개 HTML: 검색창과 actions 사이에 bulk-fee-bar 추가
  - 6개 actions.js: applyBulkFee___ 함수 추가 + btn-bulk-fee 이벤트 바인딩 + Enter키 지원
  - 기존 삭제/전체삭제 모달: confirmText: '삭제' 추가 (버튼 텍스트 유지)
  - js/actions.js (상품리스트): 동일하게 confirmText: '삭제' 추가
- 동작: 값 입력 → 적용 클릭/Enter → 경고 모달("모든 상품 N개의 판매수수료를 'X'(으)로 변경할까요?") → 확인 시 전체 card.feeRate 변경 + recalc + save + render

## v40 - 상품리스트 엑셀(CSV) 가져오기 기능 추가 (2026-07-26 16:13)
- 요청: 50개 이상 상품을 일일이 입력하기 어려움 → 엑셀에서 작성 후 일괄 가져오기
- 방식: CSV (UTF-8 BOM) — 외부 라이브러리 없이 vanilla JS로 구현
- 작업:
  - js/config.js: CSV_COLUMNS 배열(11개 헤더↔키 매핑) + CSV 관련 MESSAGES 8개 추가
  - index.html: "엑셀 템플릿" + "엑셀 가져오기" 버튼 추가, 숨겨진 csv-import-input 추가
  - css/style.css: .btn-csv 클래스 (초록색 톤으로 일반 버튼과 구분)
  - js/actions.js:
    - downloadCsvTemplate(): BOM + 헤더 + 예시 2행 CSV 다운로드
    - parseCsv(text): RFC 4180 준수 CSV 파서 (따옴표, 쉼표, 줄바꿈 처리)
    - handleCsvImportFile(): 헤더 매핑 → 카드 생성 → 교체/추가 모달
    - initActions(): CSV 버튼 이벤트 바인딩 추가
- CSV 컬럼: 상품명,옵션명,원가,환율,퍼센트,링크1,링크2,로켓배송코드,N배송코드,엔와이창고바코드,판매자상품코드
- 사진은 CSV에서 제외 (가져온 후 개별 등록)
- 헤더 유연성: 템플릿 헤더명과 일치하는 컬럼만 매핑, 나머지는 무시 (순서 무관)

## v41 - CSV 인코딩 수정 + 헤더 매칭 폴백 (2026-07-26 16:22)
- 문제: 한국 엑셀에서 CSV 저장 시 기본 인코딩이 CP949(EUC-KR) → UTF-8로 읽으면 한글 깨짐 → 헤더 매칭 실패
- 수정 (js/actions.js handleCsvImportFile):
  1. FileReader.readAsText(UTF-8) → readAsArrayBuffer()로 변경
  2. FileReader.readAsText(UTF-8) → readAsArrayBuffer()로 변경
  3. TextDecoder로 UTF-8 디코딩 후 \uFFFD(치환 문자) 감지 시 EUC-KR로 폴백 (브라우저 표준 라벨)
  4. 헤더 매칭 실패 시 에러 대신 위치 기반 매핑(CSV_COLUMNS 순서)으로 폴백, 첫 행은 헤더로 건너뜀
- 참고: 브라우저 TextDecoder는 'cp949' 라벨을 인식하지 못하므로 반드시 'euc-kr' 사용 (CP949/EUC-KR 거의 동일)

## v42 - 클로드코드 검토 이슈 4개 수정 (2026-07-26 17:30)
- 클로드코드의 코드 검토에서 발견한 4개 이슈 전부 수정

- Fix #1 🔴 저장 실패 토스트 덮어쓰기 버그:
  - 원인: save() 내부에서 실패/경고 토스트 → 바로 다음 showToast(SAVED)가 덮어씀 → 사용자가 "저장되었어요"만 보고 실패를 모름
  - 수정: 7개 storage.js의 save___() 함수가 showToast를 직접 띄우지 않고 결과 객체 반환
    - { ok: true } / { ok: true, imagesRemoved: true } / { ok: false, msg: '...' }
  - 7개 card.js의 save___Card() 함수가 결과에 따라 적절한 토스트만 띄움
  - js/actions.js의 JSON/CSV 가져오기도 동일하게 result 처리 추가

- Fix #2 🟠 마스터 페이지 백업/용량초과 대응:
  - 원인: 상품리스트 js/storage.js만 백업(_backup) + 용량초과 대응이 없었음 (다른 6개 페이지는 있었음)
  - 수정: js/storage.js를 다른 6개 페이지와 같은 패턴으로 재작성
    - _backup 이중 저장
    - isStorageQuotaError() 함수
    - cleanCardsForSave() 함수 (이미지 제거 후 재시도)
    - load()에서 손상 시 백업 복구 + id 보정 (c.id || defaults.id)
  - js/config.js: IMAGE_REMOVE_ON_SAVE_FAIL: true, SAVED_WITHOUT_IMAGES, SAVE_FAIL_QUOTA 메시지 추가

- Fix #3 🟡 image 필드 XSS 방지:
  - 원인: card.image가 escapeAttr() 없이 innerHTML에 삽입 → JSON 가져오기로 외부 파일 불러올 때 XSS 가능
  - 수정: 7개 card.js의 모든 img src="${...}"에 escapeAttr() 적용
    - card.js: escapeAttr(card.image)
    - 6개 판매 card.js: escapeAttr(imgSrc), escapeAttr(thumbSrc)

- Fix #4 🟡 JSON 가져오기 id 보정:
  - 원인: 상품리스트 js/actions.js의 handleImportFile()에서 id 보정 없음 → id 없는 JSON 가져오면 여러 카드 같은 id
  - 수정: data 대신 correctedData 사용 (filter + map + id 보정 + 필드 보정)

## v43 - 클로드코드 재검토: v42 회귀 수정 + Fix #1 전체 적용 (2026-07-26 18:40)
- WORKBUDDY의 v42 수정을 클로드코드가 재검토, 회귀 버그 발견 → 클로드코드가 직접 수정

- Fix A 🔴 문법 오류로 5개 페이지 전체 먹통 (최우선 수정):
  - 원인: v42에서 coupang/esm/elevenst/nshipping/rocketgrowth-storage.js 5개 파일의
    save___() 함수를 결과 객체 반환 방식으로 바꾸면서 닫는 중괄호 `}` 2개씩 누락
    (if 블록 하나, catch(e2)+함수 블록 하나) → SyntaxError: Unexpected end of input
  - 영향: 해당 5개 storage.js 파일 전체가 파싱 실패 → saveXXX/loadXXX 등 모든 함수가
    정의조차 안 됨 → 5개 페이지(쿠팡/ESM/11번가/N배송/로켓그로스) 전부 초기화 크래시
  - 발견 방법: node --check로 전체 js 파일 문법 검사 → 5개 파일에서 동일 오류 확인
  - 수정: smartstore-storage.js(정상 파일)의 브레이스 구조를 기준으로 누락된 } 2개씩 추가
  - 검증: node --check 54개 파일 전부 통과 + 브라우저로 6개 페이지 전부 열어
    saveXXX()/loadXXX() 등 함수 존재 및 정상 렌더링 확인 (콘솔 에러 없음)

- Fix B 🟠 Fix #1(토스트 덮어쓰기)이 저장 버튼 경로에만 적용되고 나머지는 미적용:
  - 원인: v42는 saveCard/saveXXXCard 및 JSON/CSV 가져오기만 result를 확인하도록 고침.
    사진 업로드/삭제(photo.js, 완전 미수정), 자동저장(입력 시마다), 전체 접기/펼치기,
    수정모드 진입, 접기 토글, Ctrl+S 수동저장 등은 여전히 save() 결과를 무시 →
    일부는 원래 버그(성공 토스트로 실패 가려짐) 그대로, 일부는 오히려 더 나빠짐
    (이전엔 최소 실패 토스트는 떴는데 이젠 아무 알림도 없음)
  - 수정: js/utils.js에 공용 헬퍼 reportSaveResult(result, messages, successMsg?) 추가
    - ok:true + imagesRemoved → SAVED_WITHOUT_IMAGES 토스트
    - ok:true + successMsg 있음 → successMsg 토스트 (없으면 조용히 성공)
    - ok:false → result.msg 토스트
  - 7개 페이지(상품리스트 + 6개 판매 페이지) 전체 save 호출부(총 166곳: 정의 7개 제외)를
    reportSaveResult 경유로 통일. 특히 photo.js(사진 업로드/삭제)도 이번에 처음 반영됨
  - 자동저장처럼 매번 토스트를 띄우면 시끄러운 경로는 successMsg 생략 → 성공은 조용히,
    실패만 알림
  - 파이썬 스크립트로 6개 판매 페이지의 동일 패턴 132곳을 일괄 변환 (페이지당 정확히
    동일한 함수 구조라 정규식 치환 안전하게 적용 가능, 드라이런으로 카운트 확인 후 적용)

- Fix C 🟢 오타 수정: smartstore-storage.js 주석 "다번 저장 시도 방지" → "다음 저장 시도 방지"

- 검증: node --check 전체 통과, save*() 호출 166곳 = reportSaveResult 호출 166곳 일치 확인,
  브라우저로 7개 페이지 전부 열어 실제 저장 실패/이미지제거/정상저장 3가지 시나리오를
  localStorage.setItem 몽키패치로 시뮬레이션하여 토스트 메시지가 각각 올바르게 뜨는지 확인.
  상품리스트 페이지의 백업 복구(손상된 데이터 → _backup에서 자동 복구)도 재확인.

## v43 재검토 완료 (2026-07-26 18:14)
- WORKBUDDY에서 클로드 코드의 v43 수정 내용을 검토
- Fix A (5개 storage 문법오류): 필수 수정, 우리 v42에서 누락한 `}` 문법오류 → 5개 페이지 크래시
- Fix B (reportSaveResult 전체 적용): 가장 중요한 개선, 우리는 saveCard만 수정했으나 실제 save 호출부는 166개 → 헬퍼로 전부 통일
- Fix C (오타): trivial
- 새로운 회귀 없음, beforeunload toast 무의미(기능상 문제없음), CSV import onceHandler는 기존 이슈
