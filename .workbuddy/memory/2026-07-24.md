# 2026-07-24

## v10 - 모듈 분리 (SRP 적용) (2026-07-24 13:42)
- 단일 index.html에서 HTML/CSS/JS 분리
- 디렉토리 구조:
  - index.html (HTML 구조만)
  - css/style.css (모든 스타일)
  - js/config.js (설정/상수 - 하드코딩 제거)
  - js/utils.js (escapeAttr, formatNumber, generateId, parseNum)
  - js/state.js (state, newCard, findCard, findCardIndex)
  - js/storage.js (save, load - localStorage)
  - js/toast.js (showToast, initToast)
  - js/modal.js (showModal, initModal, closeModal, isModalOpen)
  - js/photo.js (triggerPhotoUpload, removePhoto)
  - js/search.js (검색 기능, cardSearchText, matchesQuery)
  - js/card.js (render, renderCard, makeField, computeTotal - 카드 렌더링)
  - js/menu.js (드롭다운 메뉴, 동적 항목 생성)
  - js/actions.js (내보내기/가져오기/전체삭제/카드추가/삭제)
  - js/app.js (init, 키보드 단축키, 부팅)
- 하드코딩 제거: CONFIG 객체로 통합 관리
  - STORAGE_KEY, IMAGE_MAX_SIZE, TOAST_DURATION, COLUMNS
  - CURRENCY (¥/₩ 기호), FIELDS (라벨/타입/플레이스홀더), CARD_LAYOUT (필드 배치)
  - MENU_ITEMS (메뉴 항목), MESSAGES (모든 토스트/모달 메시지)
- 스크립트 로드 순서: 의존성 기반 (config → utils → state → toast → modal → storage → photo → search → card → menu → actions → app)
- file://에서도 동작 (ES modules 아닌 일반 script 태그)
- 기존 localStorage 데이터 호환 (STORAGE_KEY 동일)
- 각 모듈의 init* 함수는 app.js init()에서 DOMContentLoaded 후 일괄 호출

## v11 - 카드 헤더 배지 (2026-07-24 13:50)
- config.js에 CARD_HEADER_BADGE_FIELD 추가 (기본값: 'ny')
- #01 인덱스 옆에 지정한 필드값(판매자상품코드)을 배지로 표시
- 입력 시 실시간으로 헤더 배지 갱신
- css/style.css에 .card-index-wrap, .card-badge 스타일 추가

## v13 - 검색창 반절 너비 + 전체 숨기기/펼치기 버튼 (2026-07-24 14:07)
- index.html: 검색창 같은 줄에 `#btn-toggle-all` 버튼 추가
- css/style.css: `.search-bar input` 너비 50%로 축소, `.btn-toggle-all` 버튼 스타일 추가
- js/config.js: `BTN_COLLAPSE_ALL`, `BTN_EXPAND_ALL`, `ALL_COLLAPSED`, `ALL_EXPANDED` 메시지 추가
- js/card.js: `toggleAllCards(collapse)` 함수 추가, `render()`에서 `updateToggleAllButton()` 호출
- js/search.js: 전체 버튼 이벤트 바인딩 및 `updateToggleAllButton()` 라벨/데이터 액션 갱신
- 동작: 모든 카드가 접힌 상태면 버튼이 '전체 펼치기', 그 외에는 '전체 숨기기'로 표시

## v16 - 검색창을 상단바 상품리스트 옆으로 이동 (2026-07-24 14:22)
- index.html: `.search-bar`를 `<main>`에서 제거하고 `<header class="topbar">`의 `.title` 다음으로 이동
- css/style.css:
  - `.topbar`에 `flex-wrap: wrap` 추가
  - `.topbar .title`에 `flex-shrink: 0` 추가
  - `.topbar .search-bar`에 흰색 배경, 작은 여백, 별도 스타일 추가 (max-width: 420px, min-width: 280px)
  - 기존 `.search-bar` 스타일은 `<main>` 영역 검색창용으로 유지 (현재 사용 안 함)

## v17 - 스마트스토어 페이지 활성화 (2026-07-24 16:25)
- 메뉴에서 "스마트스토어" 선택 시 smartstore.html로 이동
- js/config.js: MENU_ITEMS에 url 추가, smartstore ready=true
- js/menu.js: 현재 페이지 URL 기준 active 표시 및 페이지 이동 처리
- 신규 파일 (SRP 모듈 분리):
  - smartstore.html
  - css/smartstore.css
  - js/smartstore-config.js (스마트스토어 전용 설정/메시지)
  - js/smartstore-state.js (smartstoreState, 카드/복수품 항목 모델)
  - js/smartstore-storage.js (save/load, 상품리스트 데이터 읽기)
  - js/smartstore-search.js (전체 필드 검색, 전체 숨기기/펼치기)
  - js/smartstore-card.js (카드 렌더링, 상품리스트 코드 매칭, 자동 계산)
  - js/smartstore-actions.js (카드/복수품 추가삭제, 내보내기/가져오기/전체삭제)
  - js/smartstore-app.js (페이지 초기화, 키보드 단축키)
- 핵심 기능:
  - 단품: 판매자상품코드 입력 시 상품리스트에서 사진/상품명/옵션명/총합(최종원가) 자동 로드
  - 복수품: "+ 복수품 추가"로 전환 후 여러 판매자상품코드 입력, 각 총합의 합계를 최종원가로 계산
  - 판매수수료: 비율(예: 0.1) 입력 시 판매가*비율 결과를 아래 칸에 자동 표시
  - 최종이익 = 판매가 - 최종원가 - 판매수수료금액 - 창고택배비 + 마켓택배비
- 상단바는 상품리스트와 동일: 내보내기/가져오기/전체삭제, 전체숨기기, 검색

## v18 - 상품리스트 원가 변경 시 스마트스토어 최종원가 자동 반영 (2026-07-24 16:57)
- 문제: 상품리스트에서 원가를 수정해도 스마트스토어 최종원가가 갱신되지 않음
- 원인: smartstore-app.js에서 resolveSmartstoreCards()가 호출되지 않았고, localStorage 변경 감지 없음
- 수정:
  - smartstore-app.js: initSmartstore()에서 loadSmartstore() 후 resolveSmartstoreCards() + saveSmartstore() 호출 추가
  - smartstore-app.js: window 'storage' 이벤트 리스너 추가 — 다른 탭에서 상품리스트 데이터가 변경되면 자동 재연동
- 효과: 페이지 로드 시마다 최신 상품리스트 데이터로 최종원가 재계산, 다른 탭에서 수정 시 실시간 반영

## v19 - 복수품 항목에 판매자상품코드 총합 표시 (2026-07-24 17:11)
- 문제: 복수품 항목의 판매자상품코드 입력칸이 카드 끝까지 넓게 펼쳐져 있고, 매칭된 상품의 총합이 보이지 않음
- 수정:
  - smartstore-config.js: itemTotal 필드 추가 (라벨: '총합')
  - smartstore-card.js: renderBundleItem()에서 코드 입력칸과 총합 표시칸을 50:50로 배치
  - smartstore.css: .bundle-item-fields, .bundle-code-field, .bundle-total-field 스타일 추가
- 효과: 복수품 항목마다 매칭된 판매자상품코드의 총합을 바로 확인 가능

## v20 - 복수품 항목별 '+' 버튼 제거 (2026-07-24 17:14)
- 문제: 복수품 항목마다 '+' 버튼이 있는데 아래에 '+ 복수품' 버튼이 있어서 중복
- 수정:
  - smartstore-card.js: renderBundleItem()에서 per-row addBtn 제거 (remove 버튼만 유지)
  - smartstore.css: .bundle-add 버튼 스타일 제거
- 효과: 복수품 항목에 '-' 버튼만 남고, 항목 추가는 아래의 '+ 복수품' 버튼으로 통일

## v21 - 스마트스토어 페이지 이동 후 데이터 유실/손상 방어 (2026-07-24 17:20)
- 문제: 스마트스토어에서 저장 후 다른 페이지로 이동했다가 돌아오면 저장 내용이 사라지거나 이상해짐
- 수정:
  - smartstore-storage.js: loadSmartstore()에서 복수품 항목도 newBundleItem() 기본값과 병합하여 누락 필드 방지
  - smartstore-card.js: resolveSmartstoreCards()에서 상품리스트 값이 비어 있을 때 기존 데이터(수동 입력) 보존
  - smartstore-card.js: 상품리스트 조회 시 데이터를 한 번만 읽어오도록 최적화 (lookupProductlistByCodeFrom)
  - smartstore-app.js: pageshow 이벤트 추가 — 브라우저 뒤로가기(bfcache 복원) 시에도 재연동
  - smartstore-app.js: beforeunload 이벤트 추가 — 페이지 이탈 전 미저장 데이터 강제 저장
- 효과: 페이지 이동/복원 시 데이터 손실 가능성 감소, 상품리스트 빈 값으로 인한 덮어쓰기 방지

## v22 - 스마트스토어 데이터 유실 루트 원인 해결 (2026-07-24 17:32)
- 문제: 저장 완료 후 다른 페이지로 이동했다가 돌아오면 복수품 모드/사진/상품명/입력값 등이 사라짐
- 원인:
  - 상품리스트 조회 실패 시 기존 데이터를 ''로 초기화하는 코드 (updateSingleProductFromCode, updateBundleItemFromCode)
  - 상품리스트 데이터가 아예 없을 때도 resolveSmartstoreCards()가 실행되어 자동 필드 덮어쓰기
  - 저장 직전 DOM 입력값을 상태에 동기화하지 않아 change 이벤트 미발생 값 누락
  - localStorage 손상 시 복구 메커니즘 부재
- 수정:
  - smartstore-card.js: 조회 실패/코드 비어있을 때 기존 image/name/option/total/finalCost 절대 지우지 않음
  - smartstore-card.js: resolveSmartstoreCards()에서 상품리스트 데이터가 없으면 즉시 리턴
  - smartstore-card.js: saveSmartstoreCard() 직전 syncSmartstoreCardFromDOM()으로 DOM 값을 상태에 동기화
  - smartstore-storage.js: saveSmartstore() 시 자동 백업(_backup 키) 생성
  - smartstore-storage.js: loadSmartstore()에서 데이터 손상/누락 시 백업에서 복구, 객체가 아닌 항목 필터링
  - smartstore-app.js: cards가 비어있고 백업이 있으면 복구 후 저장
- 효과: 상품리스트 코드 매칭 실패/데이터 일시 누락 시에도 스마트스토어 저장 데이터 보존, 페이지 이동 후에도 유지

## v23 - 스마트스토어 localStorage 용량 초과 해결 (2026-07-24 17:41)
- 문제: 저장 시 "Failed to execute 'setItem' on 'Storage': Setting the value of 'smartstore_v1' exceeded the quota" 에러
- 원인: 스마트스토어 카드/복수품 항목에 상품 사진(base64)을 그대로 저장해 localStorage 용량 초과
- 수정:
  - js/smartstore-storage.js:
    - saveSmartstore()에서 저장 전 cleanSmartstoreCardsForSave()로 카드/복수품의 image 필드를 제거
    - isStorageQuotaError() 헬퍼로 용량 초과 에러 구분
    - 용량 초과 시 SAVE_FAIL_QUOTA 메시지 출력
    - loadSmartstore()에서도 불러온 데이터의 image 필드를 비워 메모리/저장공간 절약
  - js/smartstore-card.js:
    - getProductlistImage(code) 헬퍼 추가
    - renderSmartstorePhotoRow(), renderBundleItem()에서 카드/항목의 image 대신 상품리스트에서 코드로 실시간 조회
    - updateSingleProductFromCode(), updateBundleItemFromCode()에서 image 저장 로직 제거
    - resolveSmartstoreCards()에서 image 덮어쓰기 제거
  - js/smartstore-config.js: SAVE_FAIL_QUOTA 메시지 추가
- 효과: 스마트스토어 저장 데이터에 base64 이미지가 포함되지 않아 용량 대폭 감소, 저장 실패 해결
- 참고: 상품리스트 자체의 이미지 용량이 너무 크면 여전히 전체 localStorage 공간 부족 가능. 필요 시 상품리스트 사진 정리 또는 이미지 압축 검토

## v24 - 스마트스토어 사진 클릭 업로드 복원 (2026-07-24 17:50)
- 문제: 스마트스토어 카드/복수품의 사진 영역을 클릭하면 파일 업로드가 실행되지 않음
- 원인: v23에서 localStorage 용량 절약을 위해 이미지 저장을 완전히 끊어버려 사진 클릭 이벤트도 사라짐
- 수정:
  - js/smartstore-config.js:
    - IMAGE_MAX_SIZE_BYTES(2MB), IMAGE_MAX_WIDTH/HEIGHT(800px), IMAGE_JPEG_QUALITY(0.8) 설정 추가
    - SAVED_WITHOUT_IMAGES 메시지 추가
  - js/smartstore-card.js:
    - getSmartstoreImage(card) / getBundleItemImage(item) 추가: 직접 업로드 이미지 우선, 없으면 상품리스트 실시간 조회
    - renderSmartstorePhotoRow(), renderBundleItem()에서 새 이미지 결정 함수 사용
    - 사진 영역(photo-box, bundle-thumb) 클릭 시 triggerSmartstorePhotoUpload() 실행
    - triggerSmartstorePhotoUpload(): 파일 선택 → 리사이즈/압축 → 카드/복수품 항목의 image 필드에 저장
    - resizeImageToDataURL(): canvas를 이용해 800px 이하 JPEG 0.8 품질로 리사이즈
  - js/smartstore-storage.js:
    - saveSmartstore()에서 먼저 이미지 포함 원본 데이터 저장 시도
    - 용량 초과 시 cleanSmartstoreCardsForSave()로 이미지 제거 후 재시도, SAVED_WITHOUT_IMAGES 메시지
    - loadSmartstore()에서 사용자 업로드 이미지는 보존 (상품리스트 실시간 조회값은 저장되지 않음)
  - css/smartstore.css: .photo-box, .bundle-thumb에 cursor: pointer 및 hover 스타일 추가
- 효과: 스마트스토어에서도 사진 클릭으로 업로드 가능. 이미지는 자동 리사이즈되어 용량 문제 최소화

## v25 - N배송 페이지 추가 (2026-07-24 20:36)
- 메뉴 드롭다운에 "N배송" 항목 추가 (js/config.js MENU_ITEMS)
- 신규 파일:
  - nshipping.html (스마트스토어와 동일 레이아웃, css/smartstore.css 재사용)
  - js/nshipping-config.js (N배송 전용 설정, STORAGE_KEY: 'nshipping_v1')
  - js/nshipping-state.js (nshippingState, 카드/복수품 모델)
  - js/nshipping-storage.js (save/load, 상품리스트 데이터 읽기, 백업/복구)
  - js/nshipping-search.js (전체 필드 검색, 전체 숨기기/펼치기)
  - js/nshipping-card.js (카드 렌더링, 상품리스트 코드 매칭, 자동 계산, 사진 클릭 업로드)
  - js/nshipping-actions.js (카드/복수품 추가삭제, JSON 보내기/가져오기/전체삭제)
  - js/nshipping-app.js (페이지 초기화, 상품리스트 변경 감지, bfcache 복구, 단축키)
- 기능:
  - 스마트스토어와 동일한 단품/복수품 카드, 판매자상품코드 매칭, 최종원가/판매수수료/최종이익 자동 계산
  - 상품리스트 데이터 변경 시 실시간 재연동 (storage 이벤트)
  - 사진 클릭 업로드 + canvas 리사이즈(800px, JPEG 0.8) + 용량 초과 시 이미지 없이 저장
- N배송 데이터는 'nshipping_v1' 키로 독립 저장 (스마트스토어/상품리스트와 분리)

## v26 - 복수품 추가 버튼 캡슐 디자인 (2026-07-24 20:45)
- css/smartstore.css:
  - `.bundle-add button` 및 `.bundle-add-item`을 회색 캡슐 버튼으로 변경
  - border-radius: 999px, 배경 #f5f5f7, 테두리 #e5e5ea, 그림자 효과, hover 시 배경/그림자 강화
  - `.bundle-add-item`은 가운데 정렬, 고정 최소 너비 140px, 보라색 `.btn-add` 스타일 덮어쓰기
- js/smartstore-config.js: `BTN_ADD_BUNDLE`/`BTN_ADD_ITEM` 텍스트를 전각 ＋에서 반각 +로 변경
- js/nshipping-config.js: 동일하게 버튼 텍스트 반각 +로 변경 (N배송도 smartstore.css를 재사용하므로 디자인 자동 적용)

## v27 - N배송 전용 비용 필드 추가 (2026-07-24 20:51)
- js/nshipping-config.js: FIELDS에 `barcodeFee`(NY바코드비용), `pickingFee`(피킹비용), `tagFee`(태그비용) 추가
- js/nshipping-state.js: newNshippingCard()에 세 비용 필드 기본값 추가
- js/nshipping-card.js:
  - `computeNshippingFinalProfit()`에 세 비용을 모두 차감하는 공식 적용
    - 판매가 - 최종원가 - 판매수수료 - 창고택배비 + 마켓택배비 - NY바코드비용 - 피킹비용 - 태그비용 = 최종이익
  - `renderNshippingCalcRows()`에 세 필드로 구성된 3행 추가 (창고택배비/마켓택배비 하단)
  - `syncNshippingCardFromDOM()`에 세 필드 포함
- js/nshipping-search.js: `nshippingCardSearchText()`에 세 필드 추가
- 스마트스토어 파일은 전혀 수정하지 않음 (smartstore-card.js 계산식 그대로 유지)



