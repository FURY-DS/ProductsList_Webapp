## v47 - 스마트스토어 '2개이상구매' 필드 추가 (2026-07-28 20:05)
- 요청: '최종이익' 칸을 반으로 줄이고 옆에 '2개이상구매' 칸 추가, 수식: 판매가 - 최종원가 - 판매수수료
- 작업:
  - js/smartstore-config.js: multiBuyProfit 필드 정의 추가 (label: '2개이상구매', readonly, highlight)
  - js/smartstore-state.js: 새 카드 기본값에 multiBuyProfit: '' 추가
  - js/smartstore-card.js:
    - computeSmartstoreMultiBuyProfit() 추가
    - recalcSmartstoreCard()에서 multiBuyProfit 계산
    - renderSmartstoreCalcRows() 2행을 field-row split-profit 4열로 변경 (창고택배비 | 마켓택배비 | 최종이익 | 2개이상구매)
    - updateSmartstoreCalcDisplay()에서 multiBuyProfit DOM 값 갱신
  - css/smartstore.css: .field-row.split-profit grid-template-columns: 1fr 1fr 0.5fr 0.5fr 추가, 모바일 1fr 대응
- 테스트: JS syntax check 통과
- 비고: coupang/esm/elevenst 페이지에는 아직 반영하지 않음 (사용자가 '일단 스마트스토어'로 요청)

## v48 - 모든 페이지 소수점 표시 2자리 제한 (2026-07-28 20:10)
- 요청: 모든 페이지에서 소수점이 10자리 이상 표현되는 문제, 최대 2자리까만 표시
- 원인:
  - 카드 페이지: readonly 계산 필드가 String(safeVal)로 직접 렌더링되어 부동소수점 오차 그대로 노출
  - settlement: formatNum()에 maximumFractionDigits 옵션 없음 → 10+ 자리 표시
- 작업:
  - js/utils.js:
    - parseNum()에 콤마 제거 추가
    - formatNumber()에서 n === 0일 때도 '0' 반환하도록 변경
    - round2() 헬퍼 함수 추가
  - js/card.js: computeTotal()에 round2 적용, makeField() readonly number 필드에 formatNumber 적용
  - js/smartstore-card.js: 계산 함수 4개 round2 적용, makeSmartstoreField() readonly number 필드에 formatNumber 적용
  - js/coupang-card.js: 동일하게 계산 함수 + makeCoupangField() 포맷 적용
  - js/esm-card.js: 동일하게 계산 함수 + makeEsmField() 포맷 적용
  - js/elevenst-card.js: 동일하게 계산 함수 + makeElevenstField() 포맷 적용
  - js/nshipping-card.js: 동일하게 계산 함수 + makeNshippingField() 포맷 적용
  - js/rocketgrowth-card.js: 동일하게 계산 함수 + makeRocketgrowthField() 포맷 적용
  - js/settlement-state.js: recalcMonth()에서 calc 결과를 round2()로 반올림
  - js/settlement-render.js: formatNum()에 { maximumFractionDigits: 2 } 옵션 추가
  - js/settlement-actions.js: focus 핸들러에서 소수점 2자리 반올림
- 테스트: 전체 60개 JS syntax check 통과
